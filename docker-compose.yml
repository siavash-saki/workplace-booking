services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app.db:/app/backend/app.db

  frontend:
    build: ./frontend
    ports:
      - "5173:4173"
    depends_on:
      - backend
